const menuButton=document.querySelector('.menu-button'),nav=document.querySelector('.site-nav');
if(menuButton&&nav){menuButton.addEventListener('click',()=>{const open=nav.classList.toggle('open');menuButton.setAttribute('aria-expanded',String(open));menuButton.textContent=open?'닫기':'메뉴';});nav.addEventListener('click',e=>{if(e.target.closest('a')){nav.classList.remove('open');menuButton.setAttribute('aria-expanded','false');menuButton.textContent='메뉴';}});}
document.querySelectorAll('.faq-button').forEach(button=>button.addEventListener('click',()=>{const item=button.closest('.faq-item'),open=item.classList.toggle('open');button.setAttribute('aria-expanded',String(open));button.querySelector('span:last-child').textContent=open?'−':'+';}));
const form=document.querySelector('#contact-form'),message=document.querySelector('#form-message');
if(form&&message){form.addEventListener('submit',event=>{event.preventDefault();message.className='form-message';const data=new FormData(form),company=String(data.get('company')||'').trim(),name=String(data.get('name')||'').trim(),phone=String(data.get('phone')||'').trim(),service=String(data.get('service')||'').trim(),detail=String(data.get('detail')||'').trim(),consent=data.get('consent');if(company.length<2||name.length<2||phone.length<8||!service||detail.length<10||!consent){message.textContent='회사명, 담당자, 연락처, 문의 서비스와 상세 내용을 확인하고 개인정보 수집에 동의해 주세요.';message.classList.add('error');return;}message.textContent='문의가 정상적으로 접수되었습니다. 이 페이지는 포트폴리오용 가상 기업 사이트이므로 실제 전송은 이루어지지 않습니다.';form.reset();});}
