# Development Services & Scope

외주 상담 시 사용할 수 있는 기본 개발 범위입니다. 실제 견적은 화면 수보다 **업무 흐름, 사용자 종류, 데이터 구조, 상태 전이, 외부 연동, 운영 조건**을 기준으로 산정합니다.

## 1. 업무용 웹 시스템 / 관리자 기능

### 가능한 범위

- 거래처 / 회원 / 주문 / 신청 데이터 CRUD
- 검색, 필터, 정렬, pagination
- 접수 → 진행 → 완료/취소 등 상태 처리
- 관리자용 Dashboard / 목록 / 상세 / 등록 / 수정 화면
- 서버 validation과 400 / 404 / 409 오류 계약
- Excel / CSV import-export
- REST API 및 DB 연동

### 적합한 의뢰 예시

- Excel로 관리하던 거래처를 웹에서 검색·수정하고 싶다.
- 신청이나 문의를 받고 담당자가 상태를 처리하는 화면이 필요하다.
- 고객/업무/주문 데이터를 한 화면에서 관리하고 싶다.

---

## 2. Excel · CSV · 반복 업무 자동화

### 가능한 범위

- XLSX / XLS / CSV 파일 읽기
- 데이터 행·컬럼 구조 확인
- 필수값 / 이메일 / 전화번호 등 validation
- 데이터 정규화와 완전 중복 제거
- 검색 / 필터 / 일괄 변환
- 변환 결과 CSV / XLSX export
- Undo / 원본 복원과 같은 안전장치
- 반복 작업 순서를 Recipe 형태로 저장·재실행
- 필요 시 **서버 업로드 없이 브라우저에서만 처리하는 local-first 도구** 구성

### 적합한 의뢰 예시

- 매달 같은 Excel 정리 작업을 반복하고 있다.
- ERP/CRM 업로드 전에 파일 오류를 자동으로 검사하고 싶다.
- 고객/상품/재고 데이터 형식을 일정하게 바꾸고 싶다.
- 민감한 업무 파일을 외부 서버에 올리지 않는 내부 도구가 필요하다.

---

## 3. REST API / 백엔드

### 가능한 범위

- CRUD REST API
- 검색 / 정렬 / pagination
- Request / Response validation
- 상태 머신 및 비즈니스 규칙
- 조건부 UPDATE를 이용한 stale/concurrent 변경 방어
- 400 / 404 / 409 등 일관된 오류 응답
- DB 연동
- OpenAPI / Swagger 문서
- Excel export
- integration test와 CI

### 적합한 의뢰 예시

- 기존 프런트엔드에 연결할 API가 필요하다.
- 관리자 페이지에서 사용할 데이터 API가 필요하다.
- 업무 상태를 서버 규칙에 따라 안전하게 처리해야 한다.

---

## 4. 기존 웹서비스 개선

### 가능한 범위

- 오류 원인 분석 및 버그 수정
- 기존 기능 보완
- 반복 코드 / 책임 혼재 구조 개선
- 환경변수 및 credential 분리
- 레거시 API 또는 라우팅 정리
- 테스트 추가
- 단계적 프레임워크 현대화

### 기본 원칙

기존 프로젝트는 전체 재개발부터 제안하지 않습니다. 현재 문제가 있는 지점과 변경 영향 범위를 확인한 뒤 **가장 작은 안전한 수정 단위**부터 정합니다.

---

# 진행 방식

1. **현재 업무 확인** — 누가 어떤 데이터를 입력하고 어떤 결과를 사용하는지 확인
2. **상태와 규칙 확인** — 데이터가 어떤 조건에서 변경되는지 확인
3. **필수 기능 정리** — 반드시 필요한 흐름과 있으면 좋은 기능 분리
4. **범위 확정** — 포함 / 제외 / 완료 조건 명시
5. **구현 및 검증** — 핵심 흐름, validation, 예외 상황까지 확인
6. **검수 및 납품** — 실제 사용자 흐름을 확인하고 소스·실행/배포 방법 전달

# 견적에 큰 영향을 주는 요소

- 사용자 종류와 권한 수
- 화면 수보다 실제 업무 상태 전이의 복잡도
- DB 테이블 / 데이터 관계
- 외부 API, 결제, 메일 등 제3자 서비스 연동
- Excel/CSV 파일 형식의 다양성
- 대량 데이터 처리량
- 기존 코드 수정 여부
- 배포 환경과 운영 지원 범위
- 테스트 및 문서 수준

# 상담 전에 준비하면 좋은 정보

구체적인 개발 언어를 먼저 정할 필요는 없습니다. 아래 내용이 더 중요합니다.

- 현재 어떻게 업무하고 있는지
- 가장 불편하거나 시간이 오래 걸리는 부분
- 누가 사용하는지
- 어떤 데이터를 입력하는지
- 업무 상태가 어떤 순서로 변하는지
- 최종적으로 어떤 결과가 나와야 하는지
- 참고 중인 기존 서비스가 있는지
- 희망 일정

구체적인 상담 정리는 [`PROJECT-BRIEF.md`](./PROJECT-BRIEF.md)를 사용하면 됩니다.
